FROM python:3.11-slim

WORKDIR /app

# web は twscrape 非依存。git+ 指定を含まない web 専用 requirements を使う
COPY app/requirements-web.txt .
RUN pip install --no-cache-dir -r requirements-web.txt

COPY app/ .

EXPOSE 5000

# web.py 内で waitress を使って起動する
CMD ["python", "web.py"]
